{
  "name": "@nora/modules",
  "description": "This module has .sys.mjs files for Firefox Module System esp. for Actor",
  "type": "module",
  "devDependencies": {
    "@swc/cli": "0.5.2",
    "@swc/core": "^1.10.3",
    "oxlint": "^0.15.3"
  },
  "scripts": {
    "build": "swc ./src -d _dist --strip-leading-paths --out-file-extension mjs",
    "genJarManifest": "node --import @swc-node/register/esm-register genJarManifest.ts",
    "lint": "oxlint"
  }
}