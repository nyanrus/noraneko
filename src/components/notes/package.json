{
  "name": "notes",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build --emptyOutDir",
    "lint": "pnpm biome lint ./src ts,tsx",
    "preview": "vite preview"
  }
}