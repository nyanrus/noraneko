# 道具の版。`mise install` で揃う。
# - deno: feles-build(ブラウザに入るコードと同じ言語の道具)
# - ruby: ファイルを触る script(tools/lib/*.rb と tools/scripts/*.rb)。mac/Linux は precompiled が来る
[tools]
deno = "2.9.6"
ruby = "3.4"

[settings]
ruby.compile = false
